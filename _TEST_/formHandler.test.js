import {updateUI} from '../src/client/js/formHandler'

describe('testing function updateUI is defined', () => {
    test('ensure updateUI function exists', () => {
        expect(updateUI).toBeDefined();
    })
})

import {handleSubmit} from '../src/client/js/formHandler'

describe('testing function handleSubmit is defined', () => {
    test('ensure handleSubmit function exists', () => {
        expect(handleSubmit).toBeDefined();
    })
})